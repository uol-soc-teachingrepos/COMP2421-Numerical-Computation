image: continuumio/miniconda3:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

before_script:
  - conda env create -f environment.yml
  - source activate revealjs
  - pandoc --version

test:
  script:
    - pwd
    - find . -type d
    - make

pages:
  stage: deploy
  script:
    - make all
    - make build
  artifacts:
    paths:
      - public
